# Contracts

This directory is the protocol source of truth for the monorepo.

- `protocol.yaml` documents the stable topic naming, API groups, and collection response schemas.
- `*.schema.json` files validate representative payloads and HTTP collection responses.

Keep the wire contracts here before copying logic into the backend, firmware, or camera node.
